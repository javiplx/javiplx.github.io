
```
screen -S session_nname
screen -list
screen [-d] -r [session_nname]
screen -S session_name -X quit
```

# screen commands

```
c (reate window)
d (etach)
p (revious window) / n (ext window)
" (list windows)
```

```
<tab> shift across regions (panes)
S (plit vertically)
| (split horizontally)
X (exit region)
Q (uit all rgions0
```

